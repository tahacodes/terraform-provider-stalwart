---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "stalwart_asn Resource - stalwart"
subcategory: ""
description: |-
  Configures ASN and geolocation data sources for IP address lookups. This singleton object always exists: creating the resource adopts the current server settings and destroying it only removes the resource from state.
---

# stalwart_asn (Resource)

Configures ASN and geolocation data sources for IP address lookups. This singleton object always exists: creating the resource adopts the current server settings and destroying it only removes the resource from state.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `type` (String) Variant discriminator.

### Optional

- `asn_urls` (Set of String) URLs to fetch CSV file containing the IP to ASN mappings.
- `expires` (Number) How often to refresh the ASN/Geo data.
- `geo_urls` (Set of String) URLs to fetch CSV file containing the IP to country code mappings.
- `http_auth` (Attributes) The type of HTTP authentication to use (see [below for nested schema](#nestedatt--http_auth))
- `http_headers` (Map of String) Additional headers to include in HTTP requests
- `index_asn` (Number) The position of the ASN in the DNS TXT record.
- `index_asn_name` (Number) The position of the ASN Name in the DNS TXT record.
- `index_country` (Number) The position of the country code in the DNS TXT record.
- `max_size` (Number) Maximum size of the ASN/Geo data file.
- `separator` (String) The separator character used in the DNS TXT record.
- `timeout` (Number) Time after which the ASN/Geo resource fetch is considered failed.
- `zone_ip_v4` (String) The DNS zone to query for IPv4 ASN and geolocation data.
- `zone_ip_v6` (String) The DNS zone to query for IPv6 ASN and geolocation data.

### Read-Only

- `id` (String) Server-assigned identifier.

<a id="nestedatt--http_auth"></a>
### Nested Schema for `http_auth`

Required:

- `type` (String) Variant discriminator.

Optional:

- `bearer_token` (Attributes) Bearer token for HTTP Bearer Authentication (see [below for nested schema](#nestedatt--http_auth--bearer_token))
- `secret` (Attributes) Password for HTTP Basic Authentication (see [below for nested schema](#nestedatt--http_auth--secret))
- `username` (String) Username for HTTP Basic Authentication

<a id="nestedatt--http_auth--bearer_token"></a>
### Nested Schema for `http_auth.bearer_token`

Required:

- `type` (String) Variant discriminator.

Optional:

- `file_path` (String) File path to read the secret from
- `secret` (String, Sensitive) Password or secret value
- `variable_name` (String) Environment variable name to read the secret from


<a id="nestedatt--http_auth--secret"></a>
### Nested Schema for `http_auth.secret`

Required:

- `type` (String) Variant discriminator.

Optional:

- `file_path` (String) File path to read the secret from
- `secret` (String, Sensitive) Password or secret value
- `variable_name` (String) Environment variable name to read the secret from
