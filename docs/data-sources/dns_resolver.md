---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "stalwart_dns_resolver Data Source - stalwart"
subcategory: ""
description: |-
  Configures the DNS resolver used for domain lookups. This singleton object always exists: creating the resource adopts the current server settings and destroying it only removes the resource from state.
---

# stalwart_dns_resolver (Data Source)

Configures the DNS resolver used for domain lookups. This singleton object always exists: creating the resource adopts the current server settings and destroying it only removes the resource from state.



<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `attempts` (Number) Number of times a resolution request will be retried before it is considered failed
- `concurrency` (Number) Number of concurrent resolution requests that can be made at the same time
- `enable_edns` (Boolean) Whether to enable EDNS (Extension Mechanisms for DNS) support
- `id` (String) Always `singleton`.
- `preserve_intermediates` (Boolean) Whether to preserve the intermediate name servers in the DNS resolution results
- `servers` (Attributes List) List of custom DNS server URLs to use for resolution (see [below for nested schema](#nestedatt--servers))
- `tcp_on_error` (Boolean) Whether to try using TCP for resolution requests if an error occurs during a UDP resolution request
- `timeout` (Number) Time after which a resolution request will be timed out if no response is received
- `type` (String) Variant discriminator.
- `use_tls` (Boolean) Whether to use TLS for DNS resolution

<a id="nestedatt--servers"></a>
### Nested Schema for `servers`

Read-Only:

- `address` (String) Address of the DNS server
- `port` (Number) Port of the DNS server
- `protocol` (String) Protocol to use for DNS queries
